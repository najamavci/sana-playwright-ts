README.md
# SANA Playwright (TypeScript)

## Setup
1) Install deps:
```bash
npm install
npx playwright install
```
Configure base URL:
export SANA_BASE_URL="https://your-sana-url.example"
Run tests
npm test
Run with UI
npm run test:ui
# sana-playwright-ts
